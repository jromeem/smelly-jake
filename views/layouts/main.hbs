<!DOCTYPE html>
<html>
<head>
    <title>hi jake <3</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            height: 100vh;
            background: linear-gradient(45deg, #FF5733, #33FF57); /* This is a gradient from orange to green. */
            color: white;
        }
        .contain {
            display: flex;
            flex-direction: column;
            padding: 0px 100px;
        }
        .main-card {
            background-color: rgba(255, 255, 255, 0.1); /* Slightly transparent white */
            border-radius: 20px; /* Rounded borders */
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.3); /* Drop shadow */
            padding: 20px;
            max-width: 600px;
            margin: 50px auto; /* Center the card */
        }
        .aux-card {
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            padding: 10px;
            margin: 20px 0; /* Spacing between cards */
            display: inline-block; /* To have them side-by-side, you can adjust based on preference */
        }
        .aux-card img {
            max-width: 100%; /* Ensure images don't overflow the card */
            border-radius: 10px; /* Rounded borders for images too */
        }
    </style>
</head>
<body>
    {{{body}}}
</body>
</html>
